#9. Can you change the values inside a list which is contained in set S?
s = {8, 7, 12, "Harry", [1,2]}

#no because we cant even put list in set and set is not indexed . sets ar immutable and hashable but lists are mutable and not hashable